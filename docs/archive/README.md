# 历史材料归档（非当前 Method）

2026-09-21 将原 `docs/01_*.md` 至 `27_*.md` 共27份文档移动到 [research_history](research_history/)，移动前后逐文件SHA256一致。未删除内容，可恢复；不是将全部历史研究判定失败。

| 文件编号 | 内容 | 使用边界 |
| --- | --- | --- |
| 01–04 | 原始基线、运行流程、MoE开发对照 | 可追溯证据，不代表当前完整主表 |
| 05–08 | 规则教师 | 对照背景，部分跨数据集用R0而非A4 |
| 09–13 | LLM蒸馏、语义专家与profile试验 | 历史/未保留路线，不写进当前推理链 |
| 14 | 早期AutoDL交接 | 已过期操作说明 |
| 15–24 | Jev试探、直接/类别融合与重设计 | 探索和负结果，非独立确认性评估 |
| 25–27 | 旧推荐方案、runbook、主结果计划 | 已被当前paper文档替代；包括过期状态与样本数口径 |

旧文件内的相互相对链接多数仍在同目录；指向原 `docs/` 或根目录的历史链接仅作原记录，不保证全部可点。需要当前路径请查 [论文入口](../README.md)。`docs/results/`仍保留原位，历史脚本依赖其位置，未搬动或改写结果。根目录用户草稿`部分思路.md`和`autodl_a5_update.tar`原样保留。

代码中的旧实验模块暂不删除：保留复现与单元测试依赖，通过当前[METHOD](../paper/METHOD.md)的启用/排除清单隔离。B-Tanh是历史有界残差gate“旧A5”，不是当前A5-Jev。
